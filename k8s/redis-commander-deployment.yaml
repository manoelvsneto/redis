apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-commander-deployment
  namespace: redis
  labels:
    app: redis-commander
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-commander
  template:
    metadata:
      labels:
        app: redis-commander
    spec:
      containers:
      - name: redis-commander
        image: spryker/redis-commander:0.9.0
        env:
        - name: REDIS_HOSTS
          value: "local:redis-service.redis:6379"
        ports:
        - containerPort: 8081
      nodeSelector:
        kubernetes.io/arch: arm64
